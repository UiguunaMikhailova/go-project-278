version: "2"

run:
  timeout: 5m

linters:
  # standard = errcheck, govet, ineffassign, staticcheck, unused
  default: standard
  enable:
    - bodyclose
    - misspell
    - revive

formatters:
  enable:
    - gofmt
    - goimports
